<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext only for known HTTP API hosts — not a global allow-all. -->
<network-security-config>
  <!-- Local Django (Expo LAN / emulator aliases) -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">192.168.29.18</domain>
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>
  <!-- AWS QA HTTP until TLS is provisioned on a real domain -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">13.207.17.117</domain>
  </domain-config>
  <base-config cleartextTrafficPermitted="false" />
</network-security-config>
