/** Shared workday / foreground sync timing for mobile UI and TrackingProvider. */
export const FIELD_TRACKING_INTERVAL_MINUTES = 10;
export const FIELD_MAX_WORKDAY_HOURS = 9;
export const FIELD_TRACKING_INTERVAL_MS = FIELD_TRACKING_INTERVAL_MINUTES * 60 * 1000;
export const FIELD_MAX_WORKDAY_MS = FIELD_MAX_WORKDAY_HOURS * 60 * 60 * 1000;
