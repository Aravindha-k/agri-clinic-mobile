/** Shown when the server auto-ended a workday after the 9-hour limit. */
export const WORKDAY_EXPIRED_ALERT_MESSAGE = "Workday expired. Start day again.";

/** Shown when visit submit or tracking requires an active workday. */
export const WORKDAY_REQUIRED_MESSAGE = "Start your workday before recording visits or syncing location.";
